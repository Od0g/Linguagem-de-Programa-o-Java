Explicação
Classe Pessoa:

    Atributos: nome (tipo String) e idade(tipo int).

    Métodos:

        fazAniversario: Aumenta a idade.
        imprimeDados: Exibe o nome e a idade da pessoa.

Classe Programa1:

    Contém o método main, que é o ponto de entrada do programa.
    Crie um objeto Pessoa, defina o nome e a idade inicial.
    Chama o método fazAniversarioalgumas vezes para simular aniversários.
    Exibe os dados antes e depois de comemorar aniversários.